# 👨‍🏫 نظام إدارة المعلمين مع Zoom - Edu-Dream Platform

## 📋 نظرة عامة

تم تطوير نظام متكامل لإدارة المعلمين مع ربطهم بحسابات Zoom المتاحة. النظام يتيح للمدير إنشاء وإدارة حسابات Zoom وربطها بالمعلمين بشكل مرن وآمن.

## 🚀 الميزات الرئيسية

### ✅ إدارة المعلمين
- عرض قائمة المعلمين مع معلوماتهم
- إنشاء معلم جديد
- تعديل بيانات المعلم
- حذف المعلم
- البحث والفلترة

### ✅ إدارة حسابات Zoom
- إنشاء حسابات Zoom متعددة
- إدارة المفاتيح والإعدادات
- تحديد الميزات المتاحة (تسجيل، نسخ نصي، غرف منفصلة، استطلاعات)
- تحديد الحدود (عدد الاجتماعات/اليوم، عدد المشاركين)
- تفعيل/إلغاء تفعيل الحسابات

### ✅ ربط المعلمين بحسابات Zoom
- ربط معلم واحد بحساب Zoom واحد
- إلغاء الربط عند الحاجة
- عرض حالة الربط لكل معلم
- منع ربط حساب واحد بأكثر من معلم

## 🛠️ المتطلبات التقنية

- Laravel 11+
- PHP 8.2+
- MySQL/PostgreSQL
- Vue.js 3
- Inertia.js

## 📁 هيكل النظام

```
app/
├── Models/
│   ├── User.php                    # نموذج المستخدم مع علاقة Zoom
│   ├── ZoomAccount.php             # نموذج حساب Zoom
│   ├── ZoomMeeting.php             # نموذج اجتماع Zoom
│   └── ZoomMeetingParticipant.php  # نموذج مشارك الاجتماع
├── Http/Controllers/Admin/
│   ├── UserController.php          # تحكم المستخدمين والمعلمين
│   └── ZoomAccountController.php   # تحكم حسابات Zoom
└── Services/
    └── ZoomService.php             # خدمة Zoom API

resources/js/Pages/Admin/
├── Teachers/
│   └── Index.vue                   # صفحة إدارة المعلمين
└── ZoomAccounts/
    └── Index.vue                   # صفحة إدارة حسابات Zoom

database/migrations/
├── create_zoom_accounts_table.php
├── add_zoom_account_id_to_users_table.php
└── add_zoom_account_id_to_zoom_meetings_table.php
```

## 🔧 التثبيت والإعداد

### 1. تشغيل Migrations
```bash
php artisan migrate
```

### 2. تشغيل Seeder
```bash
php artisan db:seed --class=ZoomAccountSeeder
```

### 3. إضافة المفاتيح في .env
```env
ZOOM_ACCOUNT_ID=m8VMK4ZyRkeAN0btuHP_mA
ZOOM_CLIENT_ID=A_YMIa68Rky5zPRCGHyxOw
ZOOM_CLIENT_SECRET=bUKVISRcjhcxMuViOj39hqzi5lt5z44n6
```

## 📖 كيفية الاستخدام

### 1. الوصول لإدارة المعلمين
```
GET /admin/teachers
```

### 2. إدارة حسابات Zoom
```
GET /admin/zoom-accounts
```

### 3. ربط معلم بحساب Zoom
```javascript
// في Vue Component
const linkZoomAccount = async () => {
    const response = await fetch(`/admin/teachers/${teacherId}/link-zoom-account`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-CSRF-TOKEN': csrfToken
        },
        body: JSON.stringify({
            zoom_account_id: accountId
        })
    });
};
```

### 4. إلغاء ربط المعلم
```javascript
const unlinkZoomAccount = async (teacher) => {
    const response = await fetch(`/admin/teachers/${teacher.id}/unlink-zoom-account`, {
        method: 'POST',
        headers: {
            'Content-Type': 'application/json',
            'X-CSRF-TOKEN': csrfToken
        }
    });
};
```

## 🌐 الواجهات المتاحة

### صفحة إدارة المعلمين
- **العنوان**: `/admin/teachers`
- **الوصف**: عرض قائمة المعلمين مع إمكانية ربطهم بحسابات Zoom
- **الميزات**:
  - عرض معلومات المعلم (الاسم، البريد، الكورسات)
  - عرض حالة ربط Zoom Account
  - أزرار ربط/إلغاء ربط
  - البحث والفلترة
  - إدارة المعلم (تعديل، حذف)

### صفحة إدارة حسابات Zoom
- **العنوان**: `/admin/zoom-accounts`
- **الوصف**: إدارة حسابات Zoom المتاحة
- **الميزات**:
  - إنشاء حساب جديد
  - تعديل الحسابات الموجودة
  - تفعيل/إلغاء تفعيل
  - عرض المعلم المرتبط
  - عرض الميزات والحدود

## 🔐 الأمان والصلاحيات

### التحقق من الصلاحيات
- الوصول مقصور على المديرين فقط
- التحقق من دور المستخدم قبل العمليات
- حماية من ربط حساب واحد بأكثر من معلم

### التحقق من البيانات
- التحقق من صحة المفاتيح
- التحقق من عدم وجود تعارض في الربط
- منع حذف الحسابات المرتبطة بمعلمين

## 📊 قاعدة البيانات

### جدول `zoom_accounts`
```sql
CREATE TABLE zoom_accounts (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    name VARCHAR(255) NOT NULL,
    email VARCHAR(255) UNIQUE NOT NULL,
    account_id VARCHAR(255) NOT NULL,
    client_id VARCHAR(255) NOT NULL,
    client_secret TEXT NOT NULL,
    is_active BOOLEAN DEFAULT TRUE,
    description TEXT,
    max_meetings_per_day INT DEFAULT 100,
    max_participants INT DEFAULT 300,
    features JSON,
    created_by BIGINT,
    updated_by BIGINT,
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);
```

### جدول `users` (محدث)
```sql
ALTER TABLE users ADD COLUMN zoom_account_id BIGINT NULL;
ALTER TABLE users ADD FOREIGN KEY (zoom_account_id) REFERENCES zoom_accounts(id);
```

## 🧪 الاختبار

### اختبار ربط المعلم
1. إنشاء معلم جديد
2. إنشاء حساب Zoom
3. ربط المعلم بالحساب
4. التحقق من الربط
5. إلغاء الربط

### اختبار إدارة الحسابات
1. إنشاء حساب جديد
2. تعديل الحساب
3. تفعيل/إلغاء تفعيل
4. حذف الحساب

## 🚨 استكشاف الأخطاء

### مشاكل شائعة

1. **خطأ في ربط المعلم**
   - تأكد من أن الحساب متاح (غير مرتبط)
   - تأكد من صحة معرف المعلم
   - تحقق من صلاحيات المستخدم

2. **خطأ في إنشاء حساب Zoom**
   - تأكد من صحة المفاتيح
   - تحقق من عدم تكرار البريد الإلكتروني
   - تأكد من صحة البيانات المطلوبة

3. **خطأ في قاعدة البيانات**
   - تأكد من تشغيل migrations
   - تحقق من صحة العلاقات
   - تحقق من وجود البيانات المطلوبة

### سجلات الأخطاء
```bash
tail -f storage/logs/laravel.log
```

## 🔄 التطوير المستقبلي

- [ ] **Webhook Integration** - استقبال تحديثات من Zoom
- [ ] **Recording Management** - إدارة التسجيلات
- [ ] **Analytics Dashboard** - لوحة تحكم إحصائية
- [ ] **Bulk Operations** - عمليات جماعية
- [ ] **API Endpoints** - نقاط نهاية API
- [ ] **Notification System** - نظام إشعارات

## 📱 الواجهة

### تصميم متجاوب
- دعم الأجهزة المحمولة
- تصميم RTL للغة العربية
- واجهة مستخدم حديثة وسهلة الاستخدام

### دعم اللغات
- العربية والإنجليزية
- ترجمة شاملة للواجهة
- تنسيق التواريخ والأرقام حسب اللغة

## 📞 الدعم

للمساعدة أو الاستفسارات:
- 📧 البريد الإلكتروني: support@edu-dream.com
- 📱 الهاتف: +966-XX-XXX-XXXX
- 💬 الدردشة: [رابط الدردشة]

---

**تم التطوير بواسطة فريق Edu-Dream** 🎓
**آخر تحديث**: 23 أغسطس 2025
