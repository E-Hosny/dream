# 🚀 دليل سريع: إضافة حساب Zoom جديد
## Quick Guide: Adding a New Zoom Account

---

## 📝 الخطوات السريعة / Quick Steps

### 1️⃣ الحصول على بيانات Zoom

1. اذهب إلى: https://marketplace.zoom.us/
2. أنشئ تطبيق **Server-to-Server OAuth**
3. احصل على:
   - `Account ID`
   - `Client ID`
   - `Client Secret`

### 2️⃣ إضافة الحساب للمنصة

#### الطريقة الأولى: لوحة التحكم (موصى بها)

```
Admin Dashboard → إدارة حسابات Zoom → إضافة حساب جديد
```

املأ النموذج:
- ✏️ اسم الحساب
- 📧 البريد الإلكتروني
- 🔑 بيانات API (Account ID, Client ID, Client Secret)
- ⚙️ الإعدادات (اختياري)

#### الطريقة الثانية: قاعدة البيانات

```sql
INSERT INTO zoom_accounts (
    name, email, account_id, client_id, client_secret,
    max_meetings_per_day, max_participants, is_active,
    created_by, updated_by, created_at, updated_at
) VALUES (
    'اسم الحساب',
    'email@domain.com',
    'YOUR_ACCOUNT_ID',
    'YOUR_CLIENT_ID',
    'YOUR_CLIENT_SECRET',
    100,
    300,
    1,
    1,
    1,
    NOW(),
    NOW()
);
```

### 3️⃣ ربط المعلم بالحساب

```
Admin Dashboard → المعلمون → [اختر المعلم] → ربط
```

أو من صفحة تعديل المعلم:
```
تعديل المعلم → اختر حساب Zoom → حفظ
```

---

## 🔑 مثال على البيانات الفعلية

عند إنشاء التطبيق في Zoom، ستحصل على:

```env
ZOOM_ACCOUNT_ID=m8VMK4ZyRkeAN0btuHP_mA
ZOOM_CLIENT_ID=A_YMIa68Rky5zPRCGHyxOw
ZOOM_CLIENT_SECRET=bUKVISRcjhcxMuViOj39hqzi5lt5z44n6
```

**⚠️ مهم**: هذه البيانات سرية! لا تشاركها مع أحد.

---

## 🛠️ إعدادات Zoom المطلوبة

### Scopes (الصلاحيات):

في تطبيق Zoom، فعّل:

- ✅ `meeting:write:admin`
- ✅ `meeting:read:admin`
- ✅ `meeting:update:admin`
- ✅ `meeting:delete:admin`
- ✅ `user:read:admin`

### OAuth Type:

استخدم **Server-to-Server OAuth** (وليس OAuth 2.0 العادي)

---

## ✅ التحقق من نجاح الإضافة

بعد إضافة الحساب:

1. **اختبار الاتصال**
   ```
   من صفحة إدارة حسابات Zoom → اضغط أيقونة 🔌
   ```

2. **ربط معلم تجريبي**
   ```
   اربط معلم بالحساب الجديد
   ```

3. **إنشاء اجتماع تجريبي**
   ```
   المعلم ينشئ اجتماع للتأكد من عمل الحساب
   ```

---

## 🔄 كيفية وضع البيانات السرية

### للحساب الافتراضي (Fallback):

في ملف `.env`:

```env
ZOOM_ACCOUNT_ID=your_account_id_here
ZOOM_CLIENT_ID=your_client_id_here
ZOOM_CLIENT_SECRET=your_client_secret_here
```

**ملاحظة**: هذا الحساب يُستخدم فقط للمعلمين غير المرتبطين بحساب محدد.

### للحسابات الإضافية:

**لا تضعها في .env!**

أضفها عبر:
- ✅ لوحة التحكم (Admin Dashboard)
- ✅ أو مباشرة في قاعدة البيانات

---

## 📊 سيناريوهات الاستخدام

### السيناريو 1: منصة صغيرة (2-3 حسابات)

```
الحساب الافتراضي (.env) → للطوارئ فقط

حساب 1: العلوم
├── 3 معلمين

حساب 2: اللغات  
├── 3 معلمين

حساب 3: الإنسانيات
├── 3 معلمين
```

**الإعداد**:
1. أضف 3 حسابات Zoom عبر لوحة التحكم
2. وزع المعلمين على الحسابات
3. احتفظ بحساب .env كـ backup

### السيناريو 2: منصة متوسطة (5+ حسابات)

```
حساب لكل تخصص:
- حساب الرياضيات
- حساب الفيزياء
- حساب الكيمياء
- حساب اللغة العربية
- حساب اللغة الإنجليزية
... إلخ
```

### السيناريو 3: منصة كبيرة (حساب لكل معلم)

```
كل معلم له حسابه الخاص:
- حساب المعلم أحمد
- حساب المعلمة فاطمة
- حساب المعلم محمد
... إلخ
```

---

## 🐛 حل المشاكل السريع

### المشكلة: "Failed to get access token"

**الحل**:
1. تحقق من صحة البيانات (Account ID, Client ID, Secret)
2. تأكد من تفعيل التطبيق في Zoom Marketplace
3. راجع الصلاحيات (Scopes)

### المشكلة: "الحساب غير نشط"

**الحل**:
```
إدارة حسابات Zoom → اضغط أيقونة ▶️ (تفعيل)
```

### المشكلة: لا يمكن حذف الحساب

**الحل**:
- أنهِ جميع الاجتماعات النشطة أولاً
- أو انتظر حتى تنتهي تلقائياً

---

## 📞 الدعم

للمزيد من المساعدة، راجع:

📖 **الدليل الشامل**: `MULTI_ZOOM_ACCOUNTS_GUIDE.md`

أو تواصل مع:
- 📧 support@edu-dream.com
- 💬 الدردشة المباشرة
- 📱 الدعم الفني

---

## 🎯 نصيحة ذهبية

**دائماً ابدأ بحساب واحد للتجربة!**

1. أضف حساب Zoom واحد
2. اربطه بمعلم واحد
3. أنشئ اجتماع تجريبي
4. إذا نجح، أضف باقي الحسابات

**السلامة أولاً!** 🛡️

---

**تم بواسطة**: فريق Edu-Dream 🎓

**التاريخ**: 9 نوفمبر 2025

